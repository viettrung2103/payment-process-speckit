spring:
  application:
    name: payment-bridge
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_bridge
    username: payment_user
    password: payment_pass
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
  jpa:
    hibernate:
      ddl-auto: validate  # Use validate in production, create for development
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  rabbitmq:
    host: localhost
    port: 5672
    username: admin
    password: admin
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
logging:
  level:
    root: INFO
    com.payment.bridge: DEBUG

payment:
  api:
    base-url: http://localhost:8081
    timeout:
      connect: 5000
      read: 2000
    retry:
      max-attempts: 5
      backoff-multiplier: 1.5
      initial-interval-ms: 500
