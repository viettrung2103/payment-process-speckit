# Default nginx configuration for scaled load balancer
# Additional server blocks can be added here if needed

# Include any additional configuration here if needed
# Currently, all configuration is handled in nginx.conf